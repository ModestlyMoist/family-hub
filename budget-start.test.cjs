const assert=require('node:assert/strict'),start=require('./budget-start.js'),model=require('./dashboard-model.js');
const c={id:'rent',name:'Rent',expectedMonthly:1000},b={categories:[c],monthly:{'2026-09':{assignments:{rent:100}}},accounts:[],transactions:[]},calc={available:()=>0};
assert.equal(start.gap(b,c,'2026-09'),900);b.monthly['2026-09'].paidBeforeTracking={rent:{amount:600,note:'Before setup'}};const before=JSON.stringify(b);assert.equal(start.gap(b,c,'2026-09'),300);assert.equal(model.summarize({budget:b},calc,'2026-09').attention[0].gap,300);assert.equal(JSON.stringify(b),before);assert.equal(start.gap(b,c,'2026-10'),1000);b.monthly['2026-09'].paidBeforeTracking.rent.amount=1000;assert.equal(start.gap(b,c,'2026-09'),0);assert.deepEqual(model.summarize({budget:b},calc,'2026-09').attention,[]);assert.equal(start.paid(b,{...c,creditAccountId:'visa'},'2026-09'),0);delete b.monthly['2026-09'].paidBeforeTracking.rent;assert.equal(start.gap(b,c,'2026-09'),900);console.log('Passed: partial/full pre-tracking payments, unchanged accounting data, new-month reset, card exclusion and marker reversal.');
