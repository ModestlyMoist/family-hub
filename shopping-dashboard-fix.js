// Shopping dashboard fixed-scroll list — v226a
(function(){
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function isHome(){try{return (window.view||view)==='home'}catch(e){return false}}
function fix(){if(!isHome())return;let card=document.querySelector('#dashboard .dash-shopping');if(!card||card.dataset.shoppingScrollFixed==='1')return;let actions=card.querySelector(':scope > .actions'),head=card.querySelector(':scope > .card-head');if(!actions||!head)return;card.dataset.shoppingScrollFixed='1';card.style.overflow='hidden';card.style.display='flex';card.style.flexDirection='column';card.style.maxHeight='330px';let need=(data.staples||[]).filter(x=>x.need),list=document.createElement('div');list.className='dash-shopping-list';list.style.cssText='min-height:0;flex:1 1 auto;overflow-y:auto;overscroll-behavior:contain;scrollbar-gutter:stable;padding-right:3px';list.innerHTML=need.length?need.map(x=>'<div class="home-shopping-row"><span>☐</span><b>'+esc(x.name)+'</b>'+((+x.qty||1)>1?'<span class="pill">×'+esc(x.qty)+'</span>':'')+'</div>').join(''):'<p class="muted">Shopping list is clear 🎉</p>';[...card.children].forEach(el=>{if(el!==head&&el!==actions&&!el.classList.contains('dash-card-kicker')&&!el.classList.contains('dash-progress')&&!el.classList.contains('dash-movers'))el.remove()});card.insertBefore(list,actions);actions.style.position='relative';actions.style.bottom='auto';actions.style.flex='0 0 auto';head.style.position='relative';head.style.top='auto';let kicker=card.querySelector(':scope > .dash-card-kicker');if(kicker){kicker.style.position='relative';kicker.style.top='auto'}
}
let t;function schedule(){clearTimeout(t);t=setTimeout(fix,80)}document.addEventListener('family-dashboard-ready',schedule);document.addEventListener('DOMContentLoaded',()=>{let r=document.querySelector('#dashboard');if(r)new MutationObserver(schedule).observe(r,{childList:true,subtree:true});schedule()});setInterval(fix,1000);
})();
